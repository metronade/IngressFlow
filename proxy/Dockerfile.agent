FROM python:3.12-slim

WORKDIR /app
COPY requirements.agent.txt .
RUN pip install --no-cache-dir -r requirements.agent.txt

COPY agent.py .

# No EXPOSE — this container makes an outbound-only connection to the
# gateway (§4.8a); it never listens for anything.
CMD ["python", "agent.py"]
